基于本机 checkout `/Users/liuzy/deepseek-harness` 的代码与文档，逐条回复。先说结论，再给依据。## 结论速览| 问题 | 结论 ||---|---|| 1. 嵌入姿态 | 整个项目是 developer preview、明确会破坏兼容；`headless` 是产品 CLI 表面，但不是给程序化集成用的稳定 API。**给 opencode 做 subagent 首选 ACP JSON-RPC stdio，其次 Python SDK；CLI headless 最后；没有 MCP server 形态** || 2. 会话续接 | `dsh --profile headless` **不支持 `--resume`**，只创建全新随机 session；核心层有 resume 能力，但多轮续接应走 SDK 常驻进程 + `session_id` || 3. 机器输出 | headless **没有 `--json`**，stdout 只有最终文本 + exit code；结构化结果请用 Python SDK 的 `RunResult`。JSONL 事件流明确不是受支持的 CLI 输出格式 || 4. 模型选择 | headless **无 `--model` flag**；per-invocation 用 `--patch` 覆盖 `agent-default-model`，或走 SDK 的 `provider`/`model` 参数。自定义 provider（如 longxia）可以选，但路由必须先在组合里注册 || 5. 并发 | 共享 `$DSH_HOME` 基本安全（每进程随机 session id、credentials 读取无锁/写入有跨进程锁）；但 profile 首次初始化有无锁的良性竞态。**无文档化并发上限**，高并发建议用常驻进程而非 N 个 headless || 6. 取消 | 第一次 SIGINT/SIGTERM 走 5s 优雅 dispose 是正确的清理路径；日志是 append-only + crash recovery，**不会留下结构性损坏**，但强杀可能丢最后一次 checkpoint 之后的事件 || 7. 网络 | `read-only` 只限制文件写入，**不限制网络**；官方没有网络策略层。敏感仓库要额外在工具层/OS 层禁网 |---## 1. 编程化集成姿态**developer preview 是整个项目声明，不是仅 headless。** 根 `README.md` 明确写着：> DeepSeek Harness is currently in _developer preview_ and is iterating rapidly. **THERE WILL BE COMPATIBILITY-BREAKING CHANGES.**同时 `AGENTS.md` 明确 pre-release、无兼容承诺，session 格式 `SESSION_FORMAT_VERSION = 0`、`no compatibility implied`。所以 headless 的"稳定性"只到"产品 CLI 有文档化行为"这一层，不是嵌入方可以锁定的 API。**`dsh --profile headless` 的文档化契约**（`apps/cli/reference/README.md`、`packages/bundle/headless/README.md`）：- 接收一个 task 位置参数；- 创建一个全新的持久化 Agent（session id 是 `session-${randomUUID()}`）；- 等待 quiescence、flush session；- stdout 打印最后一段非空 assistant 文本；- 最终 `turn/end` 为 `completed` 时 exit 0，否则 exit 1；- 不挂 Host/HTTP/Web/browser，不监听端口。这是"跑一次拿文本"，不是机器协议。**仓库里真正面向程序化/父 agent 的入口，按适配度排序：**1. **ACP（Agent Client Protocol）over JSON-RPC stdio** — 最贴合"把 DSH 当 subagent provider"。   - `examples/acp-agent/README.md`：明确 "intended for parent agents, subagent providers, and other programmatic clients"。   - `packages/acp/acp/README.md` 定义了 `initialize`、`session/new`、`session/prompt`、`session/cancel`、`session/update`、`session/request_permission`。   - 一个连接可拥有多个 session，stdout 只跑 ACP JSON-RPC 帧。   - 仓库内还有现成的 ACP 客户端 `@deepseek-ai/dsh-subagent-acp`。2. **Python SDK（`deepseek-harness-sdk`）** — 面向无人值守 coding agent。   - 它本身 spawn 一个打包好的单文件 JSON-RPC stdio runtime（`dsh-jsonrpc-agent`）。   - 返回结构化 `RunResult(session_id, final_response, finish_reason, events, notifications, session_root)`。   - 详见 `python/sdk/README.md`、`docs/user/guide/python-sdk.md`。3. **直接 JSON-RPC stdio** — 协议层是支持的（ACP 和 Python SDK 都是其上层），但不建议从零手搓，除非你确实需要自有协议客户端。4. **CLI spawn `dsh --profile headless`** — 只适合 shell/一次性任务；没有 JSON、没有 session id 输出、没有 resume，别拿它当委托协议。**MCP server 形态：没有。** 仓库里的 `@deepseek-ai/dsh-mcp-client` 是让 DSH *连接外部 MCP server 拿工具*，不是把 DSH 暴露成 MCP server。`apps/cli/reference/README.md` 也写明 "no MCP server is enabled by default"。---## 2. 会话续接**CLI 层：headless 不支持 `--resume`。**- `apps/cli/src/args.ts` 明确 `--resume` 属于被启动的 app，不属于 launcher 自己；示例里 `--resume` 配的是 `tui` profile，不是 headless。- headless 的 app 参数只有 `[task...]` 和 `--help`（`packages/bundle/headless/src/startup.ts`）。- headless 每次都用 `SessionId(`session-${randomUUID()}`)` 新建 session，flush 后退出，**不打印 session id**（`packages/bundle/headless/src/index.ts`）。**持久化位置**：headless 的 base bundle 配置是```yaml- id: session-persistence-jsonl  name: '@deepseek-ai/dsh-session-persistence-jsonl'  config:    root: !!js dshHomePath('sessions')```即 `$DSH_HOME/sessions` 下按 `<normalized-cwd>/<session-id>/session.jsonl.zstd`（默认 zstd）落盘。**核心层有 resume，但不在 headless CLI 暴露。** `ctx.sessionPersistence.load` / `agents.resume` 和 JSONL 后端的 load 能力都存在（`packages/session/session-persistence-jsonl/README.md`、`packages/core/agent-loop`）。这是 SDK/host 层面能力，不是 `dsh --profile headless --resume`。**ACP 明确不支持 resume**：`packages/acp/acp/README.md` 的 Known Limitations 写着 "Fresh sessions only — load, list, resume, delete, and fork are unsupported."**Python SDK 是官方多轮路径**：`docs/user/guide/python-sdk.md` 明确：> Reusing the same harness and session id preserves the session-owned Bash process… Use a fresh session id for an independent task; reuse an id only when the next call should continue the same durable conversation.所以结论：**多轮 = SDK 常驻进程 + 复用 `session_id`**。不要依赖解析 `~/.dsh/sessions` 的 JSONL 来恢复；格式是 v0、无兼容承诺。---## 3. 机器可读输出headless 没有 `--json`。它的输出只有：- stdout：最后一段非空 assistant 文本；- stderr：仅当 `turn/end.reason.kind === 'error'` 时输出错误 code/message，正常 run 为空；- exit code：`completed` 为 0，否则 1。文档里那条 "not a supported CLI output format" 的原文在 `examples/headless-agent/README.md`：> Snapshot suites run this directory's configuration through … an unexported test-only process that emits canonical session events as JSONL before its result record. **That stream is test infrastructure, not a supported CLI output format.**所以你要的 session id、usage、finish reason，从 headless CLI 拿不到。**机器可读请走：**- **Python SDK**：`RunResult` 直接给 `session_id`、`final_response`、`finish_reason`（`completed` / `max-tokens` / `error` 等）；usage 在 `events` 里，`assistant/message` 事件携带 `usage`（`packages/core/session/src/types.ts`）。- **ACP**：`session/prompt` 返回 committed text 和 `stopReason`；但 ACP 不把 usage 放上 wire，且只支持 fresh session。没有看到任何 "headless 计划加 `--json`" 的证据；不建议依赖非官方格式。---## 4. 模型选择**headless 没有 `--model` flag。** headless runner 读的是 `ctx.agentDefaultModel.currentSelection()`，来源是 settings 的 `agent-default-model` 段，base 默认：```yamlprovider: deepseek-officialmodel: deepseek-v4-flash```（`packages/bundle/base/cordis.patch.yml`、`packages/core/agent-default-model/src/index.ts`）**CLI headless 的 per-invocation 覆盖方式 = `--patch` overlay 覆盖 `agent-default-model` 行。** 例如：```yaml# model-flash.yml- id: agent-default-model  config:    provider: deepseek-official    model: deepseek-v4-flash``````shdsh --profile headless --patch ./model-flash.yml "run the tests"````--patch` 是 launcher flag，必须放在 app 参数（task）之前。这样每个 headless 进程可以选不同模型档位。注意：`agent-default-model` 的 **插件 config 只有 `provider`/`model`**；`reasoningEffort` 属于 settings 段，不在 patch config schema 里。所以"flash vs reasoning 档"最稳的做法是**按 model id 区分**（不同模型 id），而不是试图 per-invocation 改 reasoningEffort。**Python SDK 的 per-invocation 方式**：构造时传 `provider=`、`model=`、`max_tokens=`，这是官方程序化路径：```pythonwith DeepSeekHarness(provider="deepseek-official", model="deepseek-v4-flash", ...) as harness:    ...```**自定义 provider（longxia）可以在 headless 下选**，前提是路由先注册。base 已挂 `dsh-llm-pi-ai`（dormant），可以用 settings 注册自定义路由，例如 `$DSH_HOME/settings.yaml`：```yamlllm-pi-ai:  providers:    longxia:      apiKeyEnv: LONGXIA_API_KEY      api: openai-completions      baseURL: https://longxia.example/v1      models:        - id: longxia-flash```然后 `--patch` 覆盖：```yaml- id: agent-default-model  config:    provider: longxia    model: longxia-flash````agent-default-model` 不校验 catalog 成员（`packages/core/agent-default-model/README.md`），最终是否可用由实际发请求的 adapter 决定。如果 longxia 不是 OpenAI-compatible gateway，而是自有协议，则需要挂自己的 `LlmAdapter` 插件注册路由。---## 5. 并发安全同时 spawn N 个 headless 共享 `$DSH_HOME`，从实现看基本安全，但有几个边界：**安全点：**- **session 写入**：headless 每次用 `randomUUID()`，各进程 session id 不同，不写同一个 session 文件。JSONL 后端文档明确：  - "One live writer per session — append and repair are coordinated only inside the owning backend instance."  - "initial same-id publication remains collision-safe through POSIX no-overwrite hard link / Windows write-through rename without replacement."- **credentials 读取**：credentials 每次请求时解析、不跨请求缓存（`packages/credentials/credentials/README.md`），多进程并发读无锁竞争。- **credentials/settings 写入**：用 `dsh-atomic-write` 的跨进程 `withFileLock` + atomic replace；headless 通常只读，不写 credentials。相同 reference 并发写是 last-write-wins；进程持锁崩溃可能留孤儿锁（`packages/util/atomic-write/README.md`）。- **模块 fallback symlink 修复**：`ensureSymlink` 专门处理了并发 EEXIST 竞态（`packages/boot/app-boot/src/profile.ts`）。**需要知道的良性竞态：**- profile 首次 auto-init（`initProfile`）用 `existsSync` + `writeFileSync`，**没有锁**；并发首启可能同时写 `package.json`/`cordis.patch.yml`/`pnpm-workspace.yaml`，但写入内容相同，最终一致。- `prepareProfile` 每次都会重写 profile 根 `cordis.yml` 为同一内容，同样无锁但幂等。**预期并发上限：没有文档化数字。** 每个 headless 都是完整 Node/DSH 进程，还挂 config watcher。实际限制来自 CPU/内存和 provider 的 rate limit，不是 `$DSH_HOME` 锁。若 opencode 要做大量并行委托，更推荐：- 一个常驻 ACP/JSON-RPC 进程内开多个 session（ACP 一个连接可拥有多个 session）；- 或 Python SDK 复用同一 runtime subprocess。如果你坚持 N 个 headless，建议给每个任务独立的 workspace/session root，或按需隔离 `DSH_HOME`，避免首启竞态和遥测/设置文件互相影响。---## 6. 取消语义是的，这是文档化的正确清理路径。`apps/cli/reference/README.md`：- 第一次 `SIGINT`/`SIGTERM` 触发 graceful dispose，给插件树最多 **5 秒**；- `SIGTERM` 退出码 0，`SIGINT` 退出码 130；- **第二次信号立即强制退出**；- 若 one-shot 正常完成已经卡在 dispose 里，第一次 `Ctrl+C` 会直接作为 escalation 立即退出。`apps/cli/src/process-shutdown.ts` 写死 `PROCESS_SHUTDOWN_TIMEOUT_MS = 5_000`。有专门的 PTY e2e 测试 `apps/cli/tests/headless-shutdown.e2e.ts` 验证 "第一个 Ctrl+C 排水时第二个 Ctrl+C 强退"。**会不会留下损坏 session log？**- JSONL 是 append-only：每次 append `fsync`，写入/同步失败会回滚到之前的字节长度。- 崩溃恢复会校验完整帧；最后一段不完整帧会保留完整记录、从该帧起点截断，并补上合成的 tool/step/turn closer（`packages/session/session-persistence-jsonl/README.md` "Crash recovery"）。- 所以不会出现"结构性损坏"；强杀可能**丢失最后一个 durable checkpoint 之后的事件**，但可恢复、可加载。若走 ACP，协议内还有更干净的 `session/cancel`（`packages/acp/acp/README.md`），它会取消并等待 owned interval quiesce，`session/prompt` settle 为 `cancelled`。委托任务优先用它，而不是只靠 OS 信号。---## 7. read-only 下的网络姿态文档很明确：sandbox 只管文件系统，网络不在词汇表内。`docs/subsystems/sandbox.md`：> `SandboxMode` governs filesystem effects only. `read-only` asks the backend to deny writes … `workspace-write` permits writes under the workspace root … `danger-full-access` bypasses confinement. **Network and process visibility are outside this vocabulary.**`apps/cli/reference/README.md` 同样写：> reads and network access are not confined, while process visibility depends on the selected sandbox backend…所以 **`read-only` 不会禁网**。对敏感仓库，read-only 只能保证"不改文件"，不能保证"不读文件、不发网络请求"。模型完全可以通过 bash 跑 `curl` 或调用 `web_search` 等工具。**官方没有网络策略层。** 如果你给 opencode 委托敏感仓库，需要自己在外面加：- 禁用/不挂载网络类工具：base 里 `web_search` 默认存在，`web_fetch` 默认 disabled；可通过 patch 把 `tool-web` 等行 disable。- 但 bash 仍可发网络请求，所以仅靠工具开关不够；要硬保证，用容器/network namespace/防火墙/egress 代理把 DSH 运行时进程关进网络边界。- 注意凭据：`workspace-write`/`read-only` 都不阻止同 UID 工具进程读取 `$DSH_HOME/.credentials.yaml`；文档明确这是 "discretion, not a boundary"（`packages/credentials/credentials-local/README.md`）。敏感仓库环境不要暴露高权限凭据给 agent。---### 给 opencode 集成的实操建议1. 不要锁定 `dsh --profile headless` 的 stdout/退出码作为协议；项目 pre-release，随时会变。2. 如果 opencode 需要 subagent-like 调用，优先实现 **ACP client over JSON-RPC stdio**，或直接复用 `@deepseek-ai/dsh-subagent-acp`。3. 需要 session id/finish reason/usage 时，用 **Python SDK** 或同等 JSON-RPC 客户端，不要解析 headless 输出或 JSONL。4. 多轮复用用 SDK 常驻进程 + 固定 `session_id`；每个独立任务用新 `session_id`。5. 并行度大时用常驻进程多 session，而不是并发 spawn 大量 headless。6. 敏感仓库不要以为 `read-only` 能防网络外传；必须加 OS 级 egress 控制。